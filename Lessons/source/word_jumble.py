import ../../../../../usr/share/dict/words
from itertools import permutations
from set import Set


with open

permutations = permutations(['t', 'e', 'f', 'o', 'n'], 5)

dict_array = []
permutations_array = []

# Looping through all possible combinations
# of the jumbled word and adding them to an array
for i in list(permutations):
    permutations_array.append(i)

'''need to remove the quotations and commas from the items
in permutations_array so that it is one coherent word'''

if with open("/usr/share/dict/words") as words:
    words = words.read().split("\n")
